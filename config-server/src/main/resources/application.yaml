spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        default-label: services-configs
        git:
          uri: https://github.com/Nideevl/Car-Rent-Microservices
server:
  port: 8888